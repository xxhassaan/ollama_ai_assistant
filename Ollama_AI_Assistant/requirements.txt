# Ollama Desktop AI Assistant — Python dependencies
# Install with:  pip install -r requirements.txt

# HTTP client (Ollama API communication)
requests>=2.31.0

# Speech recognition (microphone → text)
SpeechRecognition>=3.10.0

# Offline text-to-speech
pyttsx3>=2.90

# PyAudio backend for SpeechRecognition microphone access
# On Windows/macOS: pip install pyaudio
# On Linux:         sudo apt install portaudio19-dev && pip install pyaudio
pyaudio>=0.2.13
